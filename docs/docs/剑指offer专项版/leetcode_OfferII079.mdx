---
tags:
  - 位运算
  - 数组
  - 回溯
  - 剑指offer专项版
id: leetcode_OfferII079
title: 🟡 剑指 Offer II 079. 所有子集
ques_id: OfferII079
platform: leetcode
online_link: https://leetcode-cn.com/problems/TVdhkn/
online_name: 所有子集
level: m
---

export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {children}
  </span>
);

:::tip LeetCode 提示

题目难度 <Highlight color="#ffc01e">中等</Highlight>

原题链接 [🔗 leetcode](https://leetcode-cn.com/problems/TVdhkn/)

:::

## 题解1

```java
class Solution {
    private List<List<Integer>> res = new ArrayList<>();

    public List<List<Integer>> subsets(int[] nums) {
        res.add(new ArrayList<>());

        for (int num : nums) {
            int n = res.size();
            for (int i=0; i<n; i+=1) {
                var newSubRes = new ArrayList<>(res.get(i));
                newSubRes.add(num);
                res.add(newSubRes);
            }
        }

        return res;
    }
}
```
