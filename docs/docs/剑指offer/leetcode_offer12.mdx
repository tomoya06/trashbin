---
tags:
  - 剑指offer
  - 回溯
id: leetcode_offer12
title: 🟡 剑指 Offer 12. 矩阵中的路径
ques_id: offer12
platform: leetcode
online_link: https://leetcode-cn.com/problems/ju-zhen-zhong-de-lu-jing-lcof/
online_name: 剑指 Offer 12. 矩阵中的路径
level: m
---

export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {children}
  </span>
);

:::tip LeetCode 提示

题目难度 <Highlight color="#ffc01e">中等</Highlight>

原题链接 [🔗 leetcode](https://leetcode-cn.com/problems/ju-zhen-zhong-de-lu-jing-lcof/)

:::



## 题解 1.py

```py
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        def dfs(x, y, idx):
            if idx == len(word):
                return True
            if x < 0 or x >= len(board[0]) or y < 0 or y >= len(board):
                return False 
            if board[y][x] == word[idx]:
                board[y][x] = '.'
                res = dfs(x, y-1, idx+1) or dfs(x, y+1, idx+1) or dfs(x+1, y, idx+1) or dfs(x-1, y, idx+1)
                board[y][x] = word[idx]
                return res
            return False
        for yy in range(len(board)):
            for xx in range(len(board[0])):
                if dfs(xx, yy, 0):
                    return True
        return False
```


